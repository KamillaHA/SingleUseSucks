---
const { variant = "secondary" } = Astro.props;
const buttonStyling = "text-white rounded-xl p-2 px-4 font-bold";
const variantClasses = {
  secondary: "bg-gron hover:bg-hovergron active:bg-clickgron",
};
---

<button class:list={[buttonStyling, variantClasses[variant]]}><slot /></button>
